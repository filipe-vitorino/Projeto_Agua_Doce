import 'package:flutter/material.dart';

class VerDadosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Ver Dados')),
      body: Center(
        child: Text('Tela Ver Dados', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
