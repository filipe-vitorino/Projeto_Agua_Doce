import 'package:flutter/material.dart';

class GraficosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Gráficos')),
      body: Center(
        child: Text('Tela Gráficos', style: TextStyle(fontSize: 24)),
      ),
    );
  }
}
